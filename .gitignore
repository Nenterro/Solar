# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python / Database
__pycache__/
*.pyc
*.db
*.db-wal
*.db-shm
*.log

# Scratch test scripts at root level
/*.py
/*.sql

# Secrets (never commit)
backend/dess_credentials.json
.env

# Local database backups written by the telemetry de-duplication migration
*.db.bak-*

# Backend. The telemetry service lives solely on the home server, in
# ~/Docker/solar-new/backend, and deploys by editing there and restarting the
# container. This repo is what Vercel builds, and Vercel only serves the
# frontend - tracking the backend here gave one service two copies with
# nothing keeping them in step.
/backend/
CLAUDE.local.md
